import { Inter } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });

import { createGlobalStyle } from 'styled-components';

export const lusitana = {
  className: 'your-lusitana-class',
};
